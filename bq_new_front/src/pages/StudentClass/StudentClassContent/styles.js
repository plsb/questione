import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    root: {
        width: '100%',
    },
    contentHeader: {
        padding: '8px 0px',
        marginLeft: '16px'
    }
});

export default useStyles;